// Given sales array
let sales = [2500, 4000, 1800, 3200, 5000];

// Using for loop to print each day's sales
for (let i = 0; i < sales.length; i++) {
    console.log(`Day ${i + 1}: ₹${sales[i]}`);
}
